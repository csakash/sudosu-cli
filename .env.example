# Sudosu CLI Environment Configuration Example
# Copy this file to .env and adjust values as needed

# Environment mode: 'dev' for local development, 'prod' for production
# ⚠️  WARNING: If SUDOSU_MODE is set here, it will OVERRIDE config file settings
# Comment out SUDOSU_MODE to use /config mode commands for switching
# Uncomment SUDOSU_MODE to lock into a specific mode (useful for development)

# SUDOSU_MODE=dev

# Development backend (local)
SUDOSU_DEV_BACKEND_URL=ws://localhost:8000/ws

# Production backend (Vercel)
SUDOSU_PROD_BACKEND_URL=wss://sudosu-cli-backend.vercel.app/ws

# Optional: Override backend URL regardless of mode
# SUDOSU_BACKEND_URL=ws://custom-backend:9000/ws
